# Rust
/engine/target/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local integration targets: a real host on someone's network is not a repo fact
api/tests/integration.toml

# Runtime data (METRIX_HOME defaults outside the repo, but just in case)
/metrix-data/
*.db
*.ndjson
*.ndjson.zst
