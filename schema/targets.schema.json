{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Targets",
  "type": "object",
  "properties": {
    "gap": {
      "description": "Idle between targets so shared dependencies settle.",
      "anyOf": [
        {
          "$ref": "#/$defs/Duration"
        },
        {
          "type": "null"
        }
      ]
    },
    "list": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Target"
      }
    },
    "order": {
      "$ref": "#/$defs/TargetOrder",
      "default": "as_resolved"
    }
  },
  "additionalProperties": false,
  "required": [
    "list"
  ],
  "$defs": {
    "Duration": {
      "description": "A duration such as \"30s\", \"10m\" or \"1h30m\". Units are required.",
      "type": "string",
      "examples": [
        "30s",
        "10m",
        "1h30m"
      ],
      "pattern": "^(\\d+[smh])+$"
    },
    "Target": {
      "type": "object",
      "properties": {
        "address": {
          "description": "Where to send traffic: `host:port` or `ip:port`.",
          "type": "string"
        },
        "attributes": {
          "description": "Free-form attributes carried from the resolved inventory — instance type, AZ,\nimage digest, task definition revision. Opaque to the engine; echoed into the\noutput so comparison can explain an outlier.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "host_header": {
          "description": "Sent as `Host`, and used for TLS SNI and certificate verification when going\ndirect to a container by IP. Most services route or vhost on it; a raw IP gets\na 404 or a default backend.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "Stable identity for charts and comparison — a task id, instance id, or hostname.",
          "type": "string"
        },
        "tls": {
          "$ref": "#/$defs/Tls",
          "default": {
            "enabled": false,
            "insecure_skip_verify": false
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "address"
      ]
    },
    "TargetOrder": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "as_resolved"
          ]
        },
        {
          "description": "Decouples results from sweep position: the first target pays cold-cache costs\non shared dependencies that the rest do not.",
          "type": "string",
          "const": "shuffle"
        }
      ]
    },
    "Tls": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false
        },
        "insecure_skip_verify": {
          "description": "Raises a run annotation when used.",
          "type": "boolean",
          "default": false
        },
        "sni": {
          "description": "Overrides `host_header` for SNI when they must differ.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
